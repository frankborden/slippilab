import { Vector } from '../common';
import type { CharacterData } from './character';

export const marth: CharacterData = {
  animID: 0,
  scale: 0.49,
  bubbleScale: 0.32,
  shieldOffset: new Vector(5, 40),
};
