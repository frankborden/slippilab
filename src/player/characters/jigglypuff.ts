import { Vector } from '../common';
import type { CharacterData } from './character';

export const jigglypuff: CharacterData = {
  animID: 1,
  scale: 0.24,
  bubbleScale: 0.168,
  shieldOffset: new Vector(0, 22),
};
