import { Vector } from '../common';
import type { CharacterData } from './character';

export const falco: CharacterData = {
  animID: 3,
  scale: 0.47,
  bubbleScale: 0.3,
  shieldOffset: new Vector(10, 40),
};
