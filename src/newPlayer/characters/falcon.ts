import { Vector } from '../common';
import type { CharacterData } from './character';

export const falcon: CharacterData = {
  animID: 4,
  scale: 0.485,
  bubbleScale: 0.3,
  shieldOffset: new Vector(5, 34),
};
