{
  "name": "@slippilab/root",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "local": "pnpm run --filter web db:local --file migrations/0000_unknown_grim_reaper.sql --persist-to .wrangler/state",
    "build": "pnpm run -r build",
    "dev": "pnpm run -r --parallel dev"
  }
}